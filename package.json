{
  "name": "word-legal-addin-vercel",
  "version": "1.0.0",
  "description": "Legal Review Add-in deployed on Vercel",
  "main": "api/legal-analysis.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static files'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
